# Private messaging with Socket.IO


set NODE_OPTIONS=--openssl-legacy-provider

## Running the frontend

```
npm install
npm run serve
```

### Running the server

```
cd server
npm install
npm start
```
